services:
  - type: web
    name: personal-blog
    runtime: python

    buildCommand: |
      pip install -r requirements.txt
      alembic upgrade head
    startCommand: |
      uvicorn main:app --host 0.0.0.0 --port $PORT

